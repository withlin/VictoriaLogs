[package]
name = "logstorage"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Shared storage and logsql logic for Rust VictoriaLogs components."
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
